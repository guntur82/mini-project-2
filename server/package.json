{
  "name": "server",
  "version": "1.0.0",
  "description": "```bash\r npx sequelize-cli model:generate --name user --attributes name:string,no_hp:string,alamat:string",
  "main": "app.js",
  "scripts": {
    "start": "nodemon app.js",
    "db-install": "sequelize-cli db:create && sequelize-cli db:migrate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-fileupload": "^1.4.0",
    "pg": "^8.7.3",
    "sequelize": "^6.21.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "sequelize-cli": "^6.4.1"
  }
}
